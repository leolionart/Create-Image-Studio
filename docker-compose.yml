services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8008:3001"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    env_file:
      - .env
    container_name: creative-image-studio
    restart: unless-stopped
